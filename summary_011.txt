This proposes that computer systems without a central leader are more reliable because they can keep working even when some parts fail. The trade-off is that they sometimes have conflicting data, which requires smart conflict-resolution strategies. The key is to match the level of data consistency to what the application actually needs, not just use the strongest guarantees everywhere.
